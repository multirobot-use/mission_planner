name: alvaro_evora_ual
root: ./

windows:
  - launch_uavs:
      layout: even-horizontal
      panes: 
        - uav2:
          - sshpass -p grvc ssh uav2@uav2
          - roscd mission_planner/scripts
          - rm .tmuxinator.yml
          - ln start_uavs_ual.yml .tmuxinator.yml
          - tmuxinator
        - uav3:
          - sshpass -p grvc ssh uav3@uav3
          - roscd mission_planner/scripts
          - rm .tmuxinator.yml
          - ln start_uavs_ual.yml .tmuxinator.yml
          - tmuxinator
